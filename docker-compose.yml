services:
  web:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data
      - /Users/mykim/claude/rh-slide-skill:/app/engine
    environment:
      - ENGINE_DIR=/app/engine
      - DATA_DIR=/app/data
    restart: unless-stopped
