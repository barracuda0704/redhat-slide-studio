services:
  web:
    build: .
    ports:
      - "8602:8501"
    env_file:
      - .env
    environment:
      - LOGIN_DISABLED=true
    volumes:
      - ./data:/app/data
      - ./data/projects:/app/engine/projects
      - ${HOME}/.config/gcloud:/root/.config/gcloud:ro
    restart: unless-stopped
