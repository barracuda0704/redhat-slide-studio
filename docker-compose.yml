services:
  web:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data
      - ./data/projects:/app/engine/projects
    environment:
      - LOGIN_DISABLED=true
    restart: unless-stopped
